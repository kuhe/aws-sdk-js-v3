const _GS = "GreetingStruct";
const _s = "salutation";

// smithy-typescript generated code
import { struct as __struct, TypeRegistry } from "@smithy/core/schema";

/* eslint no-var: 0 */

export const aws_protocoltests_restjson_nestedRegistry = TypeRegistry.for("aws.protocoltests.restjson.nested");
aws_protocoltests_restjson_nestedRegistry.startCapture();
export var GreetingStruct = __struct(_GS, 0, [_s], [0]);
aws_protocoltests_restjson_nestedRegistry.stopCapture();
